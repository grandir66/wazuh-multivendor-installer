# Mikrotik RouterOS Module Manifest
# Wazuh Multi-Vendor Integration

name=Mikrotik RouterOS
description=Mikrotik router monitoring with DHCP, VPN, firewall and system changes
version=2.1.0
category=Network
author=Riccardo Malagoli

# Rule ID range (to avoid conflicts)
rule_id_start=100200
rule_id_end=100299

# Dependencies
requires_python=no
requires_systemd=no

# Tags for filtering
tags=network,mikrotik,router,firewall,vpn,dhcp
