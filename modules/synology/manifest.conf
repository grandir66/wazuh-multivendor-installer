# Synology NAS Module Manifest
# Wazuh Multi-Vendor Integration

name=Synology NAS
description=Synology DSM JSON-based log integration with Python decoder
version=1.0.2
category=NAS
author=Riccardo Grandi

# Rule ID range (to avoid conflicts)
rule_id_start=100100
rule_id_end=100199

# Dependencies
requires_python=yes
requires_systemd=yes

# Ports used
listen_port=9513
protocol=UDP

# Tags for filtering
tags=nas,synology,fileserver,smb,dsm
