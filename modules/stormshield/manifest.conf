# Stormshield Firewall Module Manifest
# Wazuh Multi-Vendor Integration

name=Stormshield Firewall
description=Stormshield SNS firewall with admin access, VPN, IPS alerts and config changes
version=1.0.0
category=Firewall
author=Riccardo Grandi

# Rule ID range (to avoid conflicts)
rule_id_start=100400
rule_id_end=100499

# Dependencies
requires_python=no
requires_systemd=no

# Ports used
listen_port=514
protocol=UDP/TCP

# Tags for filtering
tags=firewall,stormshield,sns,ips,vpn,ipsec,sslvpn,admin
